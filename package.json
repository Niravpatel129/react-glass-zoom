{
  "name": "react-glass-zoom",
  "version": "1.0.7",
  "main": "dist/index.js",
  "peerDependencies": {
    "react": "^17.0.0 || ^18.0.0"
  },
  "scripts": {
    "build": "babel src --out-dir dist --copy-files"
  },
  "devDependencies": {
    "@babel/cli": "^7.0.0",
    "@babel/core": "^7.0.0",
    "@babel/preset-env": "^7.0.0",
    "@babel/preset-react": "^7.0.0"
  },
  "babel": {
    "presets": [
      "@babel/preset-env",
      "@babel/preset-react"
    ]
  }
}
